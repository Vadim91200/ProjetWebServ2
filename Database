CREATE TABLE utilisateur
(
    id INT PRIMARY KEY NOT NULL,
    role BOOLEAN NOT NULL,
    nom VARCHAR(100) NOT NULL,
    prenom VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL,
    mot_de_passe VARCHAR(255) NOT NULL,
    telephone VARCHAR(10) NOT NULL,
)


CREATE TABLE produit
(
    id INT PRIMARY KEY NOT NULL,
    nom_produit VARCHAR(100) NOT NULL,
    description VARCHAR(255),
    prix INT NOT NULL,
    image VARBINARY(MAX) NOT NULL,
    date_creation DATETIME NOT NULL,
    date_fin_offre DATETIME,
)


CREATE TABLE categorie
(
    id INT PRIMARY KEY NOT NULL,
    nom_categorie VARCHAR(100) NOT NULL,
   
)

// association categorie -> produit
CREATE TABLE categorie_correspondante
(
    id_produit INT REFERENCE KEY NOT NULL,
    id_categorie INT REFERENCE KEY NOT NULL,
    CONSTRAINT PK_Person PRIMARY KEY (id_produit, id_categorie),
)
